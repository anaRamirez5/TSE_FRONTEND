export enum RoutesApp {
  LOGIN = 'login',
  LAYOUT = 'layout',
  ADMIN = 'admin',
  COORDINATOR = 'coordinator',
  ASSIGMENTS = 'assignments',
  HISTORY = 'history',
  DRIVER = 'driver',
}
export enum EndPointRoute {
  LOGIN = 'login',
}
